cd ~/Development/n8n-claude-container && docker compose exec n8n sh
# docker compose build --no-cache # build the container if you need to
# docker compose up -d --force-recreate # force the container to be recreated if it already exists (will update dependencies)
# codex --yolo